import React from 'react';

const HomePage: React.FC = () => (
  <div className="min-h-screen flex flex-col items-center justify-center bg-bg-primary text-text-primary">
    <h1 className="text-4xl font-bold mb-4">Framework Generator</h1>
    <p className="text-lg text-text-secondary mb-8">Generate Playwright/Cypress frameworks with ease.</p>
    {/* Add navigation or main call-to-action here */}
  </div>
);

export default HomePage;
